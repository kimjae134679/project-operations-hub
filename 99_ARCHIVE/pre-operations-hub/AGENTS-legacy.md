# AGENTS.md

Workbench 작업의 실행 안내입니다. 사용자 정책·도구·프로젝트 목록의 원본은 [README.md](README.md)입니다.
정책을 이 파일에 다시 복제하지 않습니다.

## 1. 시작과 적용 범위

1. 현재 요청의 대상과 범위를 확인합니다. 참고·검토인지, 수정·반영인지 구분합니다.
2. 처음에는 README의 기반 원칙과 사용자 작업 정책을 읽습니다. 같은 세션에서 읽었고 변경이 없으면 다시 읽지 않습니다.
3. 프로젝트 작업이면 해당 프로젝트 README/AGENTS에서 원본 경로·현재 상태·중단 지시·명령을 확인합니다. Workbench의 프로젝트 표는 탐색 출발점입니다.
4. 도구는 필요할 때만 현재 세션에서 사용 가능한지, 연결·권한이 유효한지 확인합니다. 채택 상태 ACTIVE는 접속 성공의 증거가 아닙니다.
5. 현재 작업을 바꾸는 문서 충돌만 먼저 해결한 뒤 작업합니다. 관련 없는 문서 정리는 선행 조건이 아닙니다.
6. 채널·후보 도구·다른 프로젝트 팁은 관련될 때만 읽습니다.

Workbench 파일은 다른 저장소나 새 채팅에 자동으로 적용되지 않습니다.
재개 요청에 이 저장소 링크 또는 필요한 규칙을 전달하고, 프로젝트에서 상시 사용하려면 해당 AGENTS에 Workbench 링크와 적용 범위를 짧게 둡니다.
여러 프로젝트를 수정하는 일은 그에 대한 요청이 있을 때 수행합니다.

## 2. 이 저장소의 작업·검증 방법

- 저장소: `https://github.com/kimjae134679/astra-codex-workbench`, 기본 브랜치: `main`.
- 현재 구성: Markdown 문서 5개. 앱 소스, 실행기, package manifest, CI·빌드·설치 명령은 없습니다.
- 접근 조건: private 저장소를 읽을 수 있는 GitHub 연결. 반영에는 쓰기 권한이 필요합니다. 비밀값이나 별도 환경변수는 문서에 넣지 않습니다.
- 로컬 checkout 위치는 세션마다 확인합니다. 클라우드 작업 경로를 사용자 Windows PC 경로로 기록하지 않습니다.
- 수정 시 기준 commit SHA와 대상 파일을 먼저 읽고, 기존 파일 안에서 정리합니다.
- 검증은 변경 diff, Markdown 링크·코드 블록·표, 정책 보존, README/AGENTS 간 충돌, 작업별 적용 사례를 확인합니다. 앱 빌드·실기기 시험은 해당 없습니다.
- 여러 문서가 함께 바뀌면 가능하면 한 커밋으로 반영합니다. 반영 직전 최신 HEAD를 확인합니다.
- 다른 변경이 들어왔으면 최신본에서 자신의 변경을 다시 합칩니다. force push로 다른 작업을 덮지 않습니다.
- 반영 후 대상 브랜치·커밋·파일 내용을 다시 확인합니다. 완료 보고에는 변경 링크와 실제 검증 범위를 제시합니다.
- 복구가 필요하면 해당 변경을 되돌리는 커밋을 사용합니다. 다른 커밋까지 제거하는 reset/force push는 기본 복구 수단이 아닙니다.

## 3. 변경에 맞는 검증 선택

요청한 결과에 필요한 검증만 선택합니다. 빌드·설치·실기기 검증을 모든 작업의 일괄 조건으로 만들지 않습니다.

| 변경 | 필요한 검증의 예 |
|---|---|
| 문서 | 정책 의미 보존, 파일·링크·형식, 상충·중복, 재개 시 필요한 정보 |
| UI·스타일 | 관련 화면과 사용자 흐름, PC·폰 크기, 잘림·overflow·가독성·모달·뒤로가기·실행 오류 |
| 로직·저장 | 변경된 동작과 실패 경로, 관련 검사·테스트, 저장 변경이면 재시작·재진입 |
| 스키마·저장 키 | 구버전 데이터 백업/내보내기 → migration/정규화 → 복원/가져오기 → 데이터 확인 |
| APK·EXE 등 배포 파일 | 프로젝트 명령으로 빌드·패키징, 설치·실행·핵심 흐름, 필요한 서명·버전·hash 확인 |
| HTML·데이터와 native shell 분리 | 실제 콘텐츠 로딩·새로고침·캐시·저장 호환성. native 코드·설정·권한·의존성을 바꿨을 때는 shell 재빌드 영향 확인 |
| 외부 서비스·실기기 의존 | 로컬 검증과 실제 외부 연결·실기기 검증을 별도 기록. 접근 불가면 해당 단계 미검증 |

증거에는 다음을 구분합니다.

- PASS: 무엇을 어떤 코드·산출물·환경에서 확인했는가.
- FAIL: 실제로 실행했고 실패한 내용과 이유.
- 미실행/차단: 실행하지 못한 내용과 원인.
- 해당 없음: 작업 성격상 필요 없는 항목과 이유.

완료는 요청 결과가 구현되고 필요한 검증이 충족됐을 때 보고합니다.
요청에 필요한 외부 단계가 막혔으면 완료한 범위와 남은 단계를 분리합니다.
기존 기록의 `ASSIGNED → EXEC → CLAIMED → GATED → ACCEPTED`는 필요할 때 사용할 수 있으나,
작은 작업에 다섯 단계 보고를 강제하지 않습니다. CLAIMED는 검증된 완료가 아니며 ACCEPTED는 사용자 수락을 임의로 뜻하지 않습니다.

새 코드나 산출물에는 과거 PASS를 자동 상속하지 않습니다.
변경 영향과 동일한 의존성·환경·산출물 여부를 확인해 기존 증거의 유효 범위를 판단하고, 영향을 받는 검증만 다시 합니다.

## 4. 해당할 때 사용하는 기술 절차

### 빌드·패키징

대표 명령 → 환경/의존성 확인 → 관련 정적 검사·테스트 → 빌드·패키징 → 설치·실행 → 최종 산출물 확인.

- 프로젝트에서 실제 성공한 명령·작업 디렉터리·SDK/버전·서명 조건을 사용합니다. 없으면 확인 후 기록하며 성공한 적 없는 명령을 검증된 것으로 쓰지 않습니다.
- 일반 사용 진입점과 산출물 폴더를 명확히 합니다. 파일·앱·패키지 버전을 대조합니다.
- 임시 위치에서 빌드·서명·검증한 뒤 최종 위치로 복사할 수 있습니다. 서명 후 최종 파일과 검증 파일의 동일성을 확인합니다.
- 개발 서버/Metro에서 성공한 것과 독립 패키지·오프라인 동작을 구분합니다.
- 설치·서명·전송 파일은 필요하면 SHA-256과 서명자·기기·세션을 기록합니다.
- 릴리스 준비 완료는 실제 전달할 패키지를 기준으로 판단합니다.

### 데이터·입력·UI

- 계산 원본, record ID, DB/localStorage key, 기존 데이터 복원 경로를 확인한 뒤 저장 구조를 변경합니다.
- 앱 삭제 시 데이터 유지가 플랫폼상 가능한지 확인하고, 필요하면 백업·내보내기/복원 흐름을 제공합니다. 설치파일 분리만으로 보존을 보장하지 않습니다.
- 반복입력 편의 값은 필요하면 TTL을 둡니다. 알 수 없는 값은 0과 구분합니다.
- 글꼴 크기를 바꾸면 container·행·버튼·input도 확인합니다.
- 편집 UI는 선택영역 저장/복원, 명시적 삭제 대상, 한글 IME 조합 중 동작을 확인합니다. 단축키는 필요에 따라 `event.key`와 `event.code`를 함께 검토합니다.
- 자동 생성 데이터와 수작업 UI, 현재 원본과 보관본, 파생판과 정상 기준판을 구분합니다.

### Windows·원격 작업·로그

필요한 CMD/Python 진입점의 UTF-8 설정 예:

```cmd
@echo off
chcp 65001 >nul
set PYTHONUTF8=1
set PYTHONIOENCODING=utf-8
```

- PowerShell 출력 인코딩도 명시하고 한글·공백 경로는 관련 변경 시 시험합니다. 구형 도구 문제는 ASCII 임시 경로로 제한해 우회합니다.
- 원격 종료·프로세스 정리 전 연결 bridge/agent/service와 작업용 자식 프로세스를 구분합니다.
- 실제 PC가 offline이면 저장소에서 한 일과 PC에서 못 한 일을 분리합니다.
- 상태 화면에는 현재 단계·진행·대기/차단/오류를, 상세 로그에는 timestamp·run/session ID·명령·stack·raw event·실패 이유를 둡니다. 비밀값은 제거합니다.
- 큰 로그는 전체를 반복 읽지 않고 incremental/tail을 우선합니다.
- 작업 후 임시 산출물·staging secret/config를 정리하되 재현·복구·다음 수정에 필요한 파일과 연결 서비스는 보존합니다.

### 업데이트·rollback

새 버전 확인 → 다운로드 → 크기/hash/형식 검증 → 기존판 백업 → helper READY 확인 → 기존 앱 종료 → 교체 → 새 버전 실행·데이터 확인 → 성공 후 불필요 백업 정리.

- 프로세스 spawn 성공은 helper 준비 완료가 아닙니다.
- 교체 파일 검증 전 덮어쓰지 않고, 실행·데이터 확인 전 복구용 백업을 삭제하지 않습니다.
- 중간 실패 시 Known-Good로 돌아갈 경로를 확인합니다.
- 인증·서명·네트워크 흐름과 사용자 데이터 경로는 변경 이유가 있을 때만 수정합니다.

## 5. 작업 후 기록

현재 작업에서 바뀐 사실만 갱신합니다. 사실 변화가 없으면 문서 수정도 필요 없습니다.

| 원본 | 기록할 내용 |
|---|---|
| 프로젝트 README | 목적·확정 요구·현재 상태·다음 작업·사용자 준비조건·중요 결정 |
| 프로젝트 AGENTS | 정확한 source/경로·언어/SDK·실행 명령·패키징·접근 조건·Known-Good·실패 조건·검증 |
| Workbench README | 공통 사용자 정책·도구 채택 변화·프로젝트 안내·재사용 가치가 확인된 팁 |
| PROJECT_CHANNEL | 현재 요청 범위의 짧은 질문·공유·처리 결과. 형식은 해당 폴더 README 참고 |

검증 기록이 필요하면 `날짜 / commit 또는 산출물 식별자 / 환경 / 명령·흐름 / 결과 / 미검증 범위`를 해당 프로젝트 AGENTS에 짧게 남깁니다.
명령·권한·정상 기준판·재현 중인 실패 조건을 정리 과정에서 잃지 않게 하고, 이력은 Git/버전 기록으로 보존합니다.
전체 재작성은 정책·현재 실행 정보의 보존 여부를 diff로 확인합니다.
검증된 기술 팁과 개인 선호를 구분하고, 근거 없는 범용화를 하지 않습니다.
