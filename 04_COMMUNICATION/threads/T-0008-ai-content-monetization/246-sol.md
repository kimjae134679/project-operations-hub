# T-0008 — Sol sequential/TEMP handoff 246

Date: 2026-09-20 19:27 KST

## Candidate sequential lane
Queue refresh succeeded from the current Threads main checkout. All 1166 current `data/candidates/*.md` files are represented in filename-ascending order. Legacy staged filenames derive C/A/P from filename; newer unstaged discovery files derive `state:` from file metadata. Queue counts: C0 394, C1 772, A0 1166, P0 1166; no A1/P1.

No previously blocked entry had a recorded unblock condition observed to change before selecting the next unprocessed key.

Processed exactly one next filename-order candidate: `260916_이사가는아랫집` (rank 39). Public search found `https://bada.us/post/9545344`, a readable secondary mirror titled `이사간다는 아랫집 여자`; its exposed story matches the stored partial memo about villa tenants hit by jeonse fraud, joint legal action, and the downstairs resident deciding to move. However, no exact primary/original individual post URL or trustworthy unique primary-post identifier was resolved.

Result: C0 remains `BLOCKED_PROVENANCE`. Unblock condition: exact individual primary/original post URL or trustworthy unique primary-post identifier becomes publicly resolvable. A secondary mirror alone is insufficient. No C1/A/P upgrade, no fabricated body/comments/metrics/assets/OCR/rights/moderation state, and publicationAllowed=false.

## TEMP TEST ONLY lane
Completed one small stage-9 fail-closed verification unit only:
`03_PRODUCTION/_TEMP_TEST_ONLY_DO_NOT_PUBLISH/260920_이사가는아랫집_TEMP_TEST_ONLY/stage9_public_mirror_vs_primary_verification.json`.

The artifact records that a publicly readable mirror is not equivalent to primary provenance or permitted primary source-asset acquisition. It keeps `temporaryTestOnly=true`, `publicationAllowed=false`, `generatedImageFallback=false`, `coverBlur=false`, `renderAllowed=false`, and the binding slide-2+ original screenshot/media policy. It intentionally does not reproduce the mirror body.

Stage 16 live publishing/metrics remains disabled. No approved version or execution eligibility was created.

## Commit / next
Threads main commit: `d4cbabd` (`sequential_rank39_and_temp_stage9`).

Next run: refresh queue first, then process exactly one next unprocessed filename-order candidate after `260916_이사가는아랫집` unless refresh inserts an earlier unprocessed key. Continue one safe TEMP stage 9–15 unit only. Never publish or mark P1 from this lane; only `04_REVIEW_PUBLISH` may do so after human rights/privacy/safety approval.
