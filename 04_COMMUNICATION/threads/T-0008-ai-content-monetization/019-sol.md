# 019-sol — Viral Finder evidence normalization + duplicate grouping

Updated: 2026-09-14 KST

## Baseline read

Scheduled run started from current `Threads/main`:

- `ce603fad61bb632b7e65bbac9312e664f848ad5c` — scheduler entrypoint handoff wiring

Read first:

- `Threads/00_START_HERE/NEXT_RUN_HANDOFF.md`
- current recent commits
- operations-hub `018-sol.md`

## What changed

### 1. Cross-platform candidate normalization

Updated:

`app/features/discovery/sources/source-model.js`

Added a common discovery envelope through `normalizeCandidate(item)`.

It now resolves/stores:

```text
sourceId / sourceLabel / sourceFamily
adapter / collectionMode
manualCaptureRequired
bulkBodyCollectionAllowed
laneId / laneLabel
themeIds
engagementEvidence
canonicalUrl
exactDuplicateKey
sameStoryKey
publishedAt
sourceRisk
```

This is the base layer for later bulk import/review so Reddit/NAVER/X/manual community candidates are not handled with incompatible ad-hoc fields.

### 2. Observed engagement vs inferred interest

Added `engagementEvidence(item)`.

Observed values are explicitly tagged by origin:

```text
manual-observed
source-metadata
```

Supported observed metric classes:

```text
views
likes/upvotes
comments
shares
rank
```

Existing calculated signal fields such as popularity/freshness are kept separately under `inferred` and no longer need to be visually confused with actual source engagement.

Evidence level:

```text
observed
inferred-only
none
```

No synthetic metric is promoted to observed evidence.

### 3. Canonical URL + duplicate groups

Added canonical URL normalization:

- removes URL fragment
- removes common tracking params (`utm_*`, `fbclid`, `gclid`, `igshid`, `ref`, `share` etc.)
- preserves meaningful non-tracking query parameters
- normalizes hostname and trailing slash

Added:

- exact duplicate grouping
- same-story grouping
- optional explicit story group support

The first same-story implementation was too strict for title variants such as:

```text
[속보] AI 신기능 발표 영상
AI 신기능 발표
```

so title normalization was hardened to remove generic noise tokens such as:

```text
속보 / 단독 / 공식 / 영상 / 짤 / 펌 / 근황 / 화제
breaking / official / update / video / clip
```

before generating the story fingerprint.

### 4. Viral Finder UI evidence visibility

Updated:

`app/features/discovery/sources/source-review.js`

Each Viral Finder row can now show:

- source chip
- discovery lane chip
- observed engagement chip (`실측 조회/반응/댓글/공유/순위`)
- `추정 관심도만`
- `반응값 미확인`
- `완전중복 ×N`
- `같은소재 ×N`
- existing `원문 수동확인`

Row datasets now also expose normalized discovery evidence/group data for subsequent bulk-review implementation.

Updated CSS:

`app/features/discovery/sources/source-review.css`

Observed/inferred/unverified evidence gets visually distinct chip treatment.

### 5. Regression tests

Updated:

`test/discovery-source-model.test.mjs`

Coverage added for:

- tracking-param canonical URL cleanup while keeping meaningful params
- manual observed metric priority
- source metadata observed metric fallback
- inferred-only evidence state
- normalized Reddit/manual-capture candidate
- exact duplicate grouping
- same-story grouping after title-noise normalization
- source risk preservation

## Threads commits

Implementation commits in this run:

```text
0207a5c13fc79ca4f5832683fece88a7c269acbe  Normalize discovery evidence and same-story groups
e8fe52bb3f95bf4d338419bcef4bf946a7061815  Test discovery evidence normalization and grouping
f8683c25ea938e47a827de7ec7ed82456e74c19f  Show discovery evidence and duplicate groups in Viral Finder
45a4a5ab210c6e934c6a81f742e5ea4baff82418  Style discovery evidence chips
2a520b5821a273ca76a5a0d349a8cb10ff2fa124  Harden same-story title normalization
88828d87ab44a79a1b0a788852d7021eab5b192f  Advance scheduler handoff after discovery normalization work
```

## Validation state

Do **not** report this run as CI green yet.

An intermediate Actions run (`34770490557`) failed during JavaScript syntax/regression checks after the initial duplicate-group implementation. Server smoke was skipped because regression failed.

The suspected failing case was the intentionally different title variant for same-story grouping. The normalizer was corrected in `2a520b5...` and a new workflow run was triggered:

```text
34770550263
```

At handoff time it remained queued, so success was not observed yet.

The working container also attempted a shallow GitHub clone to run `npm run check`, but outbound DNS to github.com was unavailable. This is an environment limitation; GitHub Actions remains the authoritative executable verification path.

Next run must inspect the newest Actions run first and fix any remaining regression before expanding P1.

## Real discovery probe

A current public-web probe was performed across Reddit/YouTube/news-style search surfaces.

Result:

- no sufficiently strong **new + current + multi-source + verifiable-engagement** batch was found in that probe
- NAVER direct article access was blocked by robots in the web environment
- some returned examples were old, promotional, or lacked reliable public engagement metrics

Therefore none were falsely inserted into the viral feed as a new strong candidate. Existing verified discovery fixtures remain preferable until a stronger batch is found.

This follows the rule: `no visible metric → do not invent it`.

## Blockers

No code blocker.

Live publishing remains credential/human-approval dependent and must stay fail-closed.

Direct automatic bulk collection from Blind/DCInside remains intentionally prohibited; use public index metadata/user URL/screenshot/manual Capture.

## Next priority

Continue **P1 Viral Finder**, in this order:

1. inspect newest CI result and repair if needed
2. make bulk candidate import persist normalized discovery metadata from `normalizeCandidate()` rather than computing it only for rendering
3. build duplicate/same-story group review controls (`select group / collapse group / keep strongest / move group`)
4. add filters for observed-vs-inferred evidence and source risk
5. continue compliant multi-source real discovery tests

Only after P1 bulk workflow is solid move to P2 Audience Comfort reason chips/audit/batch filters.
